{
  "name": "example-processor-2",
  "input": [
    {
      "name": "parameters",
      "data_type": "JSONObject",
      "data_format": "json",
      "schema": {
        "type": "object",
        "properties": {
          "a": {"type": "number"},
          "b": {"type": "number"},
          "delay": {"type": "number"}
        },
        "required": ["a", "b", "delay"]
      }
    }
  ],
  "output": [
    {
      "name": "c",
      "data_type": "JSONObject",
      "data_format": "json",
      "schema": {
        "type": "object",
        "properties": {
          "v": {"type": "number"}
        },
        "required": ["v"]
      }
    }
  ],
  "configurations": [
    "default", 
    "nscc"
  ]
}
